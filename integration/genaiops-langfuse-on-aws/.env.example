LANGFUSE_SECRET_KEY="sk-..."
LANGFUSE_PUBLIC_KEY="pk-..."
# Note: The Langfuse portal now shows LANGFUSE_BASE_URL, but this workshop uses
# an older version of the Langfuse library that expects LANGFUSE_HOST.
# Please use LANGFUSE_HOST (not LANGFUSE_BASE_URL) - the URL value is the same.
LANGFUSE_HOST="https://..."